# Quality profile: hybrid retrieval plus guarded LLM polish.
# LLM grounding guard is required; LLM extraction remains disabled.
# Do not put keys here. Keep secrets only in local .env.
RUNTIME_PROFILE=quality_full
RESOURCE_STRICT=false
RESOURCE_MAX_IMAGE_GB=5

EXTRA_REQUIREMENTS=requirements-embeddings.txt
RETRIEVAL_MODE=hybrid
ENABLE_LOCAL_EMBEDDINGS=true
EAGER_LOCAL_EMBEDDINGS=false
DIRECT_QDRANT_PROJECTION=false
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
ENABLE_LLM=true
LLM_PROVIDER=mistral
ANSWER_SYNTHESIS_MODE=hybrid
EXTRACTION_MODE=deterministic
EXTRACTION_ENABLE_LLM=false
