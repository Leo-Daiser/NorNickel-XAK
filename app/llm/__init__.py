"""Optional LLM adapters.

The project must work without API keys, GPU or model servers.  Modules in
this package are therefore best-effort helpers used only when explicitly
enabled through environment variables.
"""
